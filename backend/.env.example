# Server
PORT=3000
NODE_ENV=development

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=ortax
DB_PASSWORD=ortax
DB_NAME=ortax

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# SMS provider (Phase 1 — stub)
SMS_PROVIDER=mock
SMS_API_KEY=

# LLM (OpenAI)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Telegram bots (one token per character)
TELEGRAM_BOT_ENABLED=false
TELEGRAM_BOT_TOKEN=                  # Yassawi bot
TELEGRAM_BOT_TOKEN_KHWARIZMI=        # Al-Khwarizmi bot
